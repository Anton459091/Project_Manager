<UserControl x:Class="Project_Manager.UserControls.CatalogControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_Manager.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <!-- Стеклянный элемент, который будет отображать каталоги -->
        <ListView x:Name="CatalogsListView" HorizontalAlignment="Left" VerticalAlignment="Top" Width="600" Height="300">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Catalog Name" DisplayMemberBinding="{Binding Title}" Width="200"/>
                    <GridViewColumn Header="Card Count" DisplayMemberBinding="{Binding Cards.Count}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Панель для добавления нового каталога -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,320,0,0">
            <TextBox x:Name="NewCatalogTextBox" Width="250" LostFocus="NewCatalogTextBox_LostFocus" HorizontalAlignment="Left" Margin="0,0,5,0"/>
            <Button x:Name="AddCatalogButton" Content="Add Catalog" Click="AddCatalogButton_Click" HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Панель для перетаскивания элементов -->
        <Border x:Name="CatalogBorder" Background="LightGray" AllowDrop="True" DragEnter="CatalogBorder_DragEnter" Drop="CatalogBorder_Drop" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" Width="600" Height="300"/>
    </Grid>
</UserControl>
